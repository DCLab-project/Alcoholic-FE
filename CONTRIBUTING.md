# FE CONTRIBUTING

## 1. 네이밍 규칙
- 페이지 컴포넌트: PascalCase
- 공통 컴포넌트: PascalCase
- hooks: `use`로 시작
- 상태 변수: 의미 중심 네이밍 사용

예시:
```bash
RecommendationCard.tsx
InventoryPage.tsx
useRecommendation.ts
```

## 2. 컴포넌트 분리 기준
아래 기준 중 2개 이상 해당하면 분리합니다.
- 재사용 가능성이 높다
- 파일 길이가 길어진다
- UI와 로직이 함께 복잡해진다
- 독립 테스트가 필요하다

## 3. UI 규칙
- 추천 카드 3개는 동일한 시각적 구조를 유지합니다.
- 추천 이유, 부족 재료, 조리 순서는 분리된 영역으로 표시합니다.
- 사용자가 수정 가능한 값은 명확히 구분합니다.
- 아이콘/이미지 사용 시 의미 전달이 우선입니다.

## 4. API 연동 규칙
- API 호출은 `services/` 또는 별도 client 계층으로 분리합니다.
- 컴포넌트 내부에 직접 fetch 로직을 길게 작성하지 않습니다.
- 응답 타입은 `types/`에 정의합니다.

## 5. PR 추가 규칙
FE PR에는 가능하면 아래를 포함합니다.
- 변경 화면 스크린샷
- 상태 변화 설명
- 사용자 흐름 영향 설명

## 6. 금지 사항
- 하드코딩된 mock 값을 본 코드에 장기간 유지하지 않기
- 하나의 컴포넌트에 과도한 비즈니스 로직 넣지 않기
- 스타일만 다른 중복 컴포넌트 만들지 않기
